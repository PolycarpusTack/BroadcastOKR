# R1 Findings — local validation rig

Every mock-vs-real discrepancy, in the order found. Triage each line
**fix-now** (ships on `readiness/r1-local-rig` as its own commit) · **backlog**
(named EPIC/story) · **accepted** (with date and reason).

**Day 1:** 2026-09-03 (agent `agent_151c233e2f56` pushed Oracle 85 / Postgres 9 at 08:15:45Z)
**Rig:** cockpit :3100 · tenant0 :3101 · Keycloak :8081 (native, see #2) · Oracle `LOCAL` @1521 (real PSI schema) · Postgres 17 `brokr_rig` @5433 · agent `local-agent`
**Dialects exercised (bridge preview, R1-2):** ☑ Postgres ☑ Oracle (thick) — via agent (R1-4): ☑ Postgres (`enc:v1:` password + `AGENT_DATA_KEY`) ☑ Oracle (thick)
**Startup mechanism for the unattended run:** user Startup-folder shortcut → `scripts/local-rig/start-rig.ps1` (Task Scheduler denied; finding 28). Oracle services: elevated `Start-Service OracleServiceLOCAL, OracleOraDB19Home1TNSListener` after a reboot.

## Pre-flight (2026-09-02 evening, before the run)

| # | Observation | Triage |
|---|---|---|
| P-1 | `docker` not on the Git-Bash PATH used by the assistant session; Docker Desktop state unknown from that shell. | check tomorrow (R1-0) |
| P-2 | `provision-instance.mjs` on `8ca9140` provisions cleanly (migrations 001–006, `.env` 0600). It writes `BRIDGE_PORT=3001` / `BRIDGE_HOST=0.0.0.0`; the rig needs 3100/3101 and 127.0.0.1 by hand. | accepted — documented in instructions; candidate `--port` flag if it bites twice |
| P-3 | Provisioned `.env` had no `BRIDGE_ENCRYPTION_KEY`; credentials would encrypt under the `BRIDGE_API_KEY` fallback. | fix-now (R1-0) — template now emits a dedicated key |

## Findings

| # | Story | Observation (verbatim errors welcome) | Triage | Ref |
|---|---|---|---|---|
| 1 | R1-0 | Docker is not installed on this PC (no Docker Desktop, no WSL distro, no Podman) and the account has no admin rights to install it. Keycloak 26.0.8 runs natively instead: zip from GitHub releases into `local-rig/keycloak/`, Temurin JDK 21 already on PATH, `kc.bat start-dev --import-realm --http-port 8081` with the realm copied to `data/import/`. Same realm file, same behaviour. | accepted (rig) — runbook: document the native-Java alternative next to the compose command | instructions §R1 step 1 |
| 2 | R1-0 | Port 8080 is owned by an unrelated Apache `httpd` on this PC, so Keycloak listens on **8081**. Issuer for both instances is `http://localhost:8081/realms/brokr`, not the :8080 the compose file and instructions state. The realm's redirect URIs (:3100/:3101) are unaffected. | accepted — runbook: make the IdP port a stated variable; compose file gets a `KC_PORT` note | instructions §R1 step 1, `keycloak-compose.yml` |
| 3 | R1-0 | Oracle 19c `LOCAL` (non-CDB, service `local`, listener 127.0.0.1:1521) already holds a **real WHATS'ON PSI schema** (hundreds of `PSI*` tables). The synthetic `psi-test-schema.oracle.sql` was therefore NOT loaded — it would collide on `CREATE TABLE PSITRANSMISSION`. Oracle runs against the real schema, which is the better test. | accepted — this is the point of R1 | — |
| 4 | R1-0 | Real PSI schema vs the test schema: `PSICHANNEL` does not exist (ORA-00942), and `PSITRANSMISSION.TX_ID_CHANNEL` is `NUMBER`, while the test schema models `psichannel.ch_id` as TEXT. Presets touching only `PSITRANSMISSION`/`PSISCHEDULE`/`PSIMATERIALPART` are fine; "Transmissions per Channel" binds a text id the real column will not accept. | backlog — test-schema fidelity + channel preset type; check the real channel table name | `bridge/whatson/templates.cjs`, `scripts/local-rig/psi-test-schema.*` |
| 5 | R1-0 | Real `PSITRANSMISSION` data spans 2025-01-01..2025-02-15 (60 rows), so every date-windowed preset ("This Month", 30-day binds) returns **0** on Oracle. Non-windowed presets return real values: Schedule Fill Rate = 85, Materials Ready = 0. Agent bindings for Oracle use the non-windowed presets. | accepted — real data, real zero | — |
| 6 | R1-0 | `psi-test-schema.postgres.sql` header does not carry the `brokr_reader` commands the Oracle header has (and the instructions promise for both). Created by hand: `CREATE ROLE brokr_reader LOGIN PASSWORD …; GRANT CONNECT ON DATABASE brokr_rig; GRANT USAGE ON SCHEMA psi; GRANT SELECT ON ALL TABLES IN SCHEMA psi`. Verified: reader SELECTs, `INSERT` → `permission denied for table psischedule`. | fix-now — add the reader commands to the Postgres header | `scripts/local-rig/psi-test-schema.postgres.sql` |
| 7 | R1-0 | Oracle `brokr_reader` created as sysdba (`ORACLE_SID=LOCAL` must be set for `/ as sysdba`; the Oracle services were all Stopped and had to be started). Grants: `CREATE SESSION` + `SELECT` on the three preset tables. Verified over TCP: presets return values; `INSERT` → `ORA-01031: insufficient privileges`. | accepted (rig note) | — |
| 8 | R1-0 | Postgres: both 16 (:5432) and 17 (:5433) run locally with `trust` auth on localhost. Rig DB is `brokr_rig` on **PG 17 / :5433**; test schema loaded cleanly (84 tx, 3 ch, 50 sch). | accepted (rig note) | — |
| 9 | R1-0 | The bridge only auto-loads `bridge/.env` (dotenv, hard-coded path). An instance's `.env` must be supplied by the process: `node --env-file=local-rig/<inst>/.env bridge/server.cjs`. The instructions say "start both bridges with their env files" without saying how, and a dev `bridge/.env` would still be merged underneath (dotenv does not override). | fix-now (docs) — state the exact start command in §R1 step 3 and `docs/operations.md`; consider a `BRIDGE_ENV_FILE` or refusing to load `bridge/.env` in cloud modes | `bridge/server.cjs:1` |
| 10 | R1-1 | **Tracer green.** Real browser (Playwright/Chromium) → `/api/auth/login` → Keycloak form → `/api/auth/callback` → `/` on both :3100 (cockpit) and :3101 (client). Keycloak with scope `openid profile email` delivered `name` ("Olive Owner", from firstName+lastName) and `email`; `upsertSsoUser` needed no change. `preferred_username` is not used and was not needed. First SSO user → `owner` (id 1), second → `member` (id 2), on each instance independently. | accepted — works as designed | `bridge/sessions.cjs` |
| 11 | R1-1 | RBAC on the real IdP session: member `POST /api/clients` → `403 {"error":"Insufficient permissions"}`; member `GET /api/clients` → 200 (read allowed). Anonymous `POST /api/kpi/execute-batch`, `/API/KPI/EXECUTE-BATCH`, and `…/execute-batch/` → 401 each (F1 holds on a real instance). `POST /api/auth/logout` → 200, then `/api/auth/me` → 401 for that session only; the owner's session on the same instance stayed valid. Session TTL is 8 h sliding / 7 d absolute — expiry not waited for (verified by code, not by clock). | accepted | — |
| 12 | R1-0 | Both bridges print `ValidationError … ERR_ERL_KEY_GEN_IPV6` to stderr at startup: the session-keyed rate limiter's `keyGenerator` falls back to raw `req.ip`, and express-rate-limit 8.x wants the `ipKeyGenerator` helper so an IPv6 client cannot rotate through a /64. The server keeps running (validation only), but this is a real, cheap gap in the cloud modes. | fix-now — use `ipKeyGenerator(req.ip)` in the fallback + test | `bridge/middleware/rateLimit.cjs` |
| 13 | R1-2 | **Green.** As owner on tenant0 via the real API: `POST /api/test-connection` ok for Oracle (`localhost:1521/local`, `brokr_reader`) and Postgres (`localhost:5433/brokr_rig`, `brokr_reader`); `POST /api/connections` stored both with `enc:v1:` ciphertext in `local-rig/tenant0/config.json`; `GET /api/health` (signed in) → `credentials: {unreadable: 0}`, drivers oracle+postgres; `GET /api/kpi/templates` → 12 presets (both dialects). Preview: Oracle "Transmissions This Month" 0, "Schedule Fill Rate" 85, "Materials Ready" 0; Postgres "This Month" 9, "Fill Rate" 90.0. Schema browser lists the three real Oracle tables and the four Postgres ones. | accepted | — |
| 14 | R1-2 | Postgres scalar results arrive as **strings** (`{"VALUE":"9"}`, `"90.0"`) — `pg` returns bigint/numeric as text — while Oracle returns numbers. The KR path is safe (`core.cjs` does `Number(Object.values(rows[0])[0])`), but preview/JSON consumers see mixed types across dialects. `NUM_ROWS` in the Postgres schema browser is `"-1"` for never-analyzed tables (PG 14+ `reltuples`). | **done 2026-09-03** — `normalizePgRows` coerces numeric strings (safe integers and decimals only); unanalyzed tables report `NUM_ROWS: null` | `bridge/whatson/core.cjs` runPgQuery |
| 15 | R1-2 | `POST /api/channels` on the real PSI schema: `PSICHANNEL` does not exist, so the handler fell through to its second query and returned channels as bare numeric ids with `name = id` (`22964`, `212832`, …). No `PSI` table carries a `CH_ID` or `CH_DESCRIPTION` column and `TX_ID_CHANNEL` has no foreign key, so the channel entity is not where the mock schema puts it — needs a WHATS'ON data-model answer, not a guess. | backlog (product) — real channel lookup for WHATS'ON; the fallback did its job | `bridge/routes/whatson.cjs` /channels |
| 16 | R1-2 | The Oracle driver ran in **thick mode**: `oracledb.initOracleClient()` (called with no `libDir`) found the 19.3 client via `C:Oracle9cdb_homein` on PATH; `oracledb.thin === false`, `oracleClientVersionString 19.3.0.0.0`. Thin mode (no Instant Client at all) has therefore *not* been exercised by this rig — a client PC without an Oracle client on PATH will take the other path. | backlog — one run with `ORACLE_CLIENT_DIR` pointing nowhere / PATH stripped, to prove the thin path; document both in `docs/operations.md` | `bridge/whatson/core.cjs:140`, `routes/whatson.cjs:92` |
| 17 | R1-2 | **Fix-now, blocks every UI story.** After the OIDC callback the SPA shows "Sign in with SSO" although `brokr_session` is set and an in-page `fetch('/api/auth/me')` returns 200. Network trace: the bundle calls `GET http://localhost:3001/api/health` and `…:3001/api/auth/me` → `ERR_CONNECTION_REFUSED`. `src/constants/config.ts` defaults `BRIDGE_URL` to `http://localhost:3001` when `VITE_BRIDGE_URL` is unset, and the documented cloud builds (`VITE_EDITION=client npx vite build`) never set it. A provisioned instance therefore serves a UI that cannot reach its own bridge; the desktop default leaked into the cloud editions. Clicking the SSO button then navigates to `:3001/api/auth/login` → `chrome-error://`. | **fix-now — done on this branch**: `BRIDGE_URL` defaults to `''` (same-origin) when `BUILD_EDITION !== 'desktop'`, `VITE_BRIDGE_URL` still overrides (explicit `''` honoured via `??`); `src/constants/__tests__/config.test.ts`. Re-probed after rebuilding the rig bundles: every call now goes to `:3101`, the Dashboard renders as Olive Owner. | `src/constants/config.ts:8`, `src/context/AuthContext.tsx:68`, `src/store/bridgeSync.ts:85` |
| 18 | R1-3 | Cockpit side works through the real API: owner on :3100 → `POST /api/clients` (Tenant Zero, id `client_tenant0`, 201 — fleet allowed in cockpit mode) → `POST /api/cockpit/tenants {clientId}` → 201 with a 64-hex token. Written to tenant0's `.env` as `BRIDGE_COCKPIT_URL=http://localhost:3100`, `BRIDGE_SHARE_TOKEN`, and `BRIDGE_SHARE_INTERVAL_MS=60000` (rig; default is 5 min). `GET /api/cockpit/metrics` → `[]` until a KR is Shared (R1-6). | accepted | `bridge/routes/cockpit.cjs` |
| 19 | R1-4 | `POST /api/goals` with `owner: "Olive Owner"` (a name) → `500 Internal server error`; log: `SqliteError: FOREIGN KEY constraint failed`. `goals.owner` is an INTEGER FK to `users(id)` and `channel` an INTEGER, which the JSON API does not say and does not validate. A frontend bug or a hand-written payload gets an opaque 500 instead of a 400 naming the field. | **done 2026-09-03** — `globalErrorHandler` answers `SQLITE_CONSTRAINT_*` with 400 `{error:'constraint_violation', constraint, detail}` | `bridge/routes/goals.cjs:137`, `bridge/middleware/errorHandler` |
| 20 | R1-4 | Agent enrolment on the real instance: owner `POST /api/agents/enrol-token` → 201 (15-min TTL); `node bridge/agent.cjs enroll …` → `agent_151c233e2f56`, identity + skeleton config written; replaying the same enrol token → `401 Invalid or expired enrolment token`. `GET /api/agents` lists it. Windows: identity file is 0644 on NTFS (documented rig limitation). | accepted | — |
| 21 | R1-4 | **Tracer green.** `node bridge/agent.cjs run --dir local-rig/agent` (interval 60 s on the rig) → `pushed 2 values`. tenant0 `GET /api/goals`: `kr-rig-oracle-fill` current=85 syncStatus=ok, `kr-rig-pg-month` current=9 syncStatus=ok, both with ISO `lastSyncAt`; `GET /api/agents` lastSeenAt updated. Oracle binding used a plaintext password, Postgres an `enc:v1:` value from `encrypt()` decrypted with `AGENT_DATA_KEY` (F9 verified on the rig). Both dialects through a read-only account. | accepted | — |
| 22 | R1-3 | Channel end-to-end: PUT the goal with `sharedWithMediagenix: true` on the Oracle KR → within one interval the cockpit's `GET /api/cockpit/metrics` shows tenant `client_tenant0` / `Tenant Zero` with `{krId, value: 85, target: 95, direction: 'hi', timestamp, receivedAt}` — the five allowlisted fields and nothing else. The Postgres KR (not shared) did not appear. | accepted | `bridge/cockpit/sharePayload.cjs` |
| 23 | R1-3 | The client bridge prints nothing at startup about the cockpit channel (`startSharePushLoop` is silent unless a push fails), so an operator cannot tell from the startup lines whether `BRIDGE_COCKPIT_URL`/`BRIDGE_SHARE_TOKEN` were picked up. Same for the agent: no line on ingest in the tenant log. | **done 2026-09-03** — startup line `Sharing opted-in KRs to <cockpit> every N min.`; one log line per accepted ingest with the unknown ids | `bridge/server.cjs:126`, `bridge/cockpit/pushLoop.cjs` |
| 24 | R1-5 | **Fix-now.** Manager creates a goal with a live KR through the real form: `POST /api/goals` 201 → `POST /api/kpi/execute-batch` 200 `{status:'ok', current:85}` → toast "Synced 1 live KR" → the card shows 85 / 95, 89 %, "synced 10:28:25" (F5's ordering holds). **Two seconds later** the 5-s change poll returns the goal from the bridge — whose KR row still says `current_val 0, sync_status pending` — and the card reverts to 0 / 95, 0 %. Sampled each second: value visible at t+0, gone from t+1 onward, never returns. `execute-batch` results only ever lived in the browser store (TD-1, hardening backlog 2026-08-31: "change-poll can silently revert these fields"); on a cloud instance with the poll always on, a user-triggered sync is therefore invisible after one poll. The agent path does not have this problem because `/api/agent/ingest` persists via `applySyncedValue`. | **fix-now — done on this branch**: execute-batch persists a result when the query *is* the KR's stored query (`storedKR`), via the same `applySyncedValue` + goal `updated_at` bump as ingest and the sync loop; owner ad hoc SQL is answered, never stored (`bridge/__tests__/execute-batch-persist.test.cjs`). Re-run on the rig: 85 / 95 stays on the card through 12 polls and a reload. Rest of TD-1 (setMonitor/toggleSubtask/addBulkTasks) stays backlog. | `bridge/routes/whatson.cjs` execute-batch, `bridge/liveSync.cjs` |
| 25 | R1-5 | F2/F4 on the rig: tenant0 restarted with a different `BRIDGE_ENCRYPTION_KEY` → stderr `WARNING: 2 stored connection password(s) cannot be read with the configured key — restored from another machine, or the key was rotated? Re-enter those passwords on the Clients page; nothing has been changed on disk.`; signed-in `/api/health` → `credentials: {unreadable: 2}`; Dashboard System Health panel shows "2 database passwords cannot be read with this installation's encryption key …"; a masked-connection test returns `ok:false`. Restored the key → no warning, `unreadable: 0`, panel clean. `config.json` SHA-256 identical before, during, and after. | accepted — works as designed | `bridge/server.cjs:157`, `SystemHealthPanel` |
| 26 | R1-5 | The unreadable-credentials warning is printed with `console.warn`, i.e. **stderr**, while the rest of the startup banner goes to stdout. An operator who redirects only stdout to the process log (as the rig did at first) never sees it; conversely the rate-limit validation error (finding 12) was in the same stream. | **done 2026-09-03** — warnings stay on stderr; stdout gets `N startup warning(s) — see stderr.`; operations.md already says capture both | `bridge/server.cjs:161-168` |
| 27 | R1-5 | Re-testing a **stored** connection always fails: `GET /api/connections` masks passwords as `***`, `toConnectionInput()` strips the `id`, and `POST /api/test-connection` then tries to log in with the literal `***` → `Connection test failed` even with the right key (reproduced on the rig after the key restore: `unreadable: 0`, yet the masked test is `ok:false`). Everything that re-tests a saved connection (ClientsPage auto-test on load, ClientRow/ClientSettingsPage "Test") shows red after a reload. | **fix-now — done 2026-09-03**: `resolveTestConnection` substitutes the stored secret for a masked/absent password when the id is known (owner-only route); `toConnectionInput` keeps the id. Rig: masked Oracle test → `ok:true`. | `bridge/routes/whatson.cjs:71`, `src/utils/connections.ts`, `src/pages/ClientsPage.tsx:84` |
| 28 | R1-6 | `schtasks /Create` → `Access is denied` on this corporate PC (no Task Scheduler rights for the user). Startup mechanism for the unattended run is a **user Startup-folder shortcut** (`shell:startupBrOKR local rig.lnk`) running `scripts/local-rig/start-rig.ps1` hidden at logon — the script is idempotent and starts Keycloak (native), both bridges, and the agent; Oracle's services still need an elevated `Start-Service` after a reboot (their startup type is Manual and cannot be changed without admin). | accepted (rig) — recorded in the header; runbook step 8 corrected | `scripts/local-rig/start-rig.ps1` |
| 29 | R1-6 | Client edition, Settings page as owner: "WHATS'ON CONNECTION — No connection bound — contact your Mediagenix operator." Provisioning seeds the pinned client with `connection_id ''` and the client edition offers no way to bind one, although the owner can create connections (finding 13) and goals can use them directly. Bound it via `PUT /api/clients/:id` for the rig. This is R6-1 territory (admin UIs) — the message is honest but the operator-side action does not exist yet. | **done 2026-09-03 (R6-1)** — the cockpit binds it through the operator channel: Clients → Tenant modal (`TenantModal`), `BRIDGE_OPERATOR_TOKEN` provisioned per client instance; the client edition keeps its message | `bridge/routes/cockpit.cjs`, `src/components/clients/TenantModal.tsx` |
| 30 | R1-7 | `.gitignore` had `local-rig/` unanchored, which also ignores `scripts/local-rig/` — the tracked rig assets survived only because they were committed before the rule; the new `scripts/local-rig/start-rig.ps1` was silently ignored (`git status` showed nothing). | fix-now — anchored to `/local-rig/`; `git check-ignore` verified both ways | `.gitignore:48` |
| 31 | R1-6 | **Fix-now (reported by Yannick from the real UI).** New Goal modal: every character typed in Title moved focus to the Close ✕. `Modal`'s focus/keydown effect depended on `onClose`, which GoalsPage passes as an inline arrow (new every render); a controlled field re-renders the page per keystroke, the effect re-ran, and its `requestAnimationFrame` focused the first focusable element — the header's Close button. Playwright's `fill()` never showed it (one change event). | **fix-now — done 2026-09-03**: `onClose` read through a ref, effect keyed on `open` only; initial focus goes to the first control in the body; RTL test types three characters with a re-rendering parent (fails on the old Modal) | `src/components/ui/Modal.tsx` |
| 32 | R1-6 | **Fix-now (reported by Yannick).** A failed "Test connection" said only `Connection test failed`; the driver's reason (wrong password, unknown service, closed port, missing database) went to the bridge log the operator cannot see. The route is owner-only, so the reason is theirs to have. | **fix-now — done 2026-09-03**: `describeConnectionError` returns `Connection test failed — <code>: <first line>` with a plain-language hint for the common codes (ECONNREFUSED, ENOTFOUND, ETIMEDOUT, 28P01, 3D000, ORA-nnnnn). | `bridge/routes/whatson.cjs` |
| 33 | R1-6 | **Fix-now (reported by Yannick, wizard on the cockpit).** "Load tables" returned nothing for the Postgres connection. The connection form defaults Schema to `PSI`; the saved Postgres connection therefore carried `schema: 'PSI'`, and `getTablesQuery`/`getColumnsQuery` compared `information_schema.table_schema = 'PSI'` case-sensitively against the lowercase `psi` schema. `/api/channels` already folded case per dialect; the table/column browsers did not. | **fix-now — done 2026-09-03**: Postgres schema folded to lower case, Oracle to upper (default: the user), in `core.cjs`; `schema-browser-case.test.cjs`. Form default stays `PSI` — it is right for Oracle. | `bridge/whatson/core.cjs` getTablesQuery/getColumnsQuery |
| 34 | R1-6 | `start-rig.ps1` started a bare `node` REPL instead of each bridge and the agent the first time it actually had to start something: the helper's parameter was named `$args`, which PowerShell reserves, so the argument list was silently dropped. The idempotent runs earlier in the day never exercised the start path. Also learned: `npm run electron:build` fails with `EPERM … better_sqlite3.node` while the rig's bridges/agent are running — stop them first (`start-rig.ps1 -Stop`), build, `npm run rebuild:node`, start again. | fix-now — parameter renamed; runbook step 8 note | `scripts/local-rig/start-rig.ps1`, `CLAUDE.md` Testing |
| 35 | R6-1 | Rig exit for R6-1 through the real cockpit UI (Playwright, Keycloak login as owner): Tenant modal registered `http://localhost:3101` with tenant0's `BRIDGE_OPERATOR_TOKEN` → "Reachable · v0.9.1 · operator token accepted"; both rig connections listed from the tenant; bound Postgres, then Oracle; "Refresh channels" pulled 4; share token re-minted (env lines shown once); enrol token minted, `agent.cjs enroll` from the shown command → `agent_8296d242fc02`, listed "never seen", revoked from the modal. No curl. Found on the way: the modal set its success message *before* its reload finished, so a selection made right after the message was overwritten by the late reload (second bind's button stayed disabled); and a previous message stayed on screen while the next action ran. | **done 2026-09-04** — `run()` refreshes first, then sets the message, and clears it when an action starts | `src/components/clients/TenantModal.tsx` |
| 36 | R6-1 | Signed-in `/api/health` on the cockpit reported `database: {tables: 0}` with a populated DB: the count excluded `name NOT LIKE '_%'`, and `_` is a LIKE wildcard, so every table was excluded. Cosmetic (System Health panel), but wrong since day one. | **done 2026-09-04** — `NOT LIKE '\_%' ESCAPE ''` | `bridge/server.cjs` /api/health |
| 37 | R6-1 | The R6-1 exit script re-minted Tenant Zero's share token from the modal "to see the env lines" — which replaced the hash on the cockpit while tenant0 kept pushing with the old token: `[share-push] cockpit refused payload: HTTP 401` every minute from 22:11 until the restart at 22:50 (fleet metrics stale for that window). Correct behaviour, but the modal said nothing about it. | **done 2026-09-04** — warning under the re-mint button; rig repaired by minting again through the UI and writing the token into `local-rig/tenant0/.env`, tenant0 restarted | `src/components/clients/TenantModal.tsx` |

## R1-5 — review-fix verification on the rig

| Check | Result |
|---|---|
| Manager: create goal with live KR → auto-sync ok (F5) | ☑ 2026-09-03 — 85 / 95 (89 %), `synced` label, persists (after finding 24 fix) |
| Manager: edit SQL → auto-sync ok | ☑ 2026-09-03 — SQL swapped to `COUNT(*)` → 20 / 95 within one round trip, persisted |
| Manager: Sync All Live KRs | ☑ 2026-09-03 — one execute-batch, `lastSyncAt` advanced, status ok |
| Manager: replayed execute-batch with altered SQL → `Not a stored query` | ☑ 2026-09-03 — per-result error, stored value untouched; the unaltered replay ran and returned 20 |
| Wizard as manager: goal step shown, connection step hidden; failed sync still "created", one goal (F6) | ☑ 2026-09-03 — Help → Run the setup wizard: steps Welcome · Bridge · What to measure · First goal · Dashboard KPI · Done (no Database, no Client); `PSI.NO_SUCH_TABLE_R1` → "✓ … created" + "Query execution failed", one goal, create button gone; the error is now also on the KR row (finding 24 fix) |
| Wrong `BRIDGE_ENCRYPTION_KEY` → startup warning + Dashboard warning; restore key → clean, disk unchanged (F2/F4) | ☑ 2026-09-03 — finding 25 |
| Agent with an `enc:v1:` password + `AGENT_DATA_KEY` (F9) | ☑ 2026-09-03 — Postgres binding (finding 21) |
| `/API/KPI/EXECUTE-BATCH` with no session → 401 on the real instance (F1) | ☑ 2026-09-03 — also `/api/kpi/execute-batch/` (finding 11) |

## Daily log (unattended run)

| Day | Date | Values arriving | Staleness honest | Backups pairing | Notes |
|---|---|---|---|---|---|
| 1 | 2026-09-03 | yes — agent every 60 s (Oracle 85 / Postgres 9), cockpit receiving the shared KR | yes — `synced HH:MM:SS` labels fresh on tenant0; no stale banner | _(check tomorrow — first scheduled backup has not run yet)_ | Rig built today: R1-0 → R1-5 done, 29 findings, 3 fix-now shipped (rate-limit IPv6, same-origin bridge URL, execute-batch persistence). Startup: Startup-folder shortcut. |
