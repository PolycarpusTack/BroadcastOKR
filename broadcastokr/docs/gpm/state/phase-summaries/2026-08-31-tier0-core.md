# Phase Summary — Tier 0: Desktop Core Complete (2026-08-31)

Branch `feature/tier0-core`, 11 commits, all 10 stories of
`tier0-backlog-2026-08-31.md` delivered. Tier 0 exit state: every store
mutation reaches the bridge; live-KR sync runs bridge-side; activity log and
backups survive restarts; concurrent edits conflict loudly; the edition seams
and no-fork guardrails exist for Tier 1.

## Delivered

- T0-1 `06d03bb` — one HTTP client (`bridgeFetch` + `retries` option); apiFetch is a fast-fail wrapper.
- T0-2 `21f24ae` — 127.0.0.1 default bind (compose opts into 0.0.0.0), timing-safe key compare,
  DB-side query cancellation (`callTimeout`/`statement_timeout`), genericized 500s, user-scoped pool keys.
- T0-3 `6bc3337` — `setMonitor`/`toggleSubtask`/`addBulkTasks` bridge writes.
- T0-4 `56b5de3` — online backup scheduler (daily, keep 14); snapshot-based `/api/sync/backup`.
- T0-5 `0c2a73b` — persistent `activity_log` (migration 002) + `/api/activity`; hydrate on connect.
- T0-6 `6f6c54d` — version-checked CAS PUTs for goals/tasks (migration 003), 409 → merge + toast,
  per-entity serialized write queue, echoed-version application, no 4xx retries.
- T0-7 `c53739c` — tenancy mode: `entitlements.ts` / `editions.cjs` sole env readers; health reports
  mode; gates at nav/route/store/server depth; client-mode single-tenant 403s.
- T0-8 `6c54f69` — FF-1 (client bundles tree-shake fleet chunks, sentinel-scanned in a CI edition
  matrix), FF-2 (domain-layer import fences; DBConnection moved to types), FF-7 (env-read +
  filename-fork bans).
- T0-9 `78b01d3` — `bridge/whatson/` extraction (core/store/templates + routes/whatson.cjs);
  server.cjs is a composition root. The Tier-2 agent boundary now exists in code.
- T0-10 `ee6fbe6` — bridge-side live-KR sync loop + `POST /api/kpi/sync-now`; client timer removed;
  progress recomputed client-side on merge (closes TD-1 entirely).

## Verification at close
206 vitest (36 files) + 72 bridge tests + 3 E2E green; lint 0; desktop/client/internal builds green
with sentinel checks (client bundle contains no fleet chunks).

## Architecture memory
- The batch value-extraction contract lives twice deliberately: `routes/whatson.cjs` (HTTP) and
  `server.cjs#executeKrQuery` (loop) — both delegate to `runQueryWithTimeout` + `buildBinds`.
- Versioned writes: only goals/tasks; clients/users/teams/templates remain last-write-wins until
  Tier 1 revisits.
- The route-contract test extracts `/api/...` string literals — path prefixes must be hoisted to
  literals (see `putVersioned`/`setMonitor` pattern); inline ternaries inside template paths break it.
- electron-builder ABI gotcha still applies (`npm rebuild better-sqlite3` after packaging).

## Open / next
Tier 1 (Client Edition) per the tiered plan: OIDC/SSO + sessions → server RBAC → AuthContext
consumer → pinning/provisioning/opt-in flag. TD-2 (modal reset suppressions) unchanged.
