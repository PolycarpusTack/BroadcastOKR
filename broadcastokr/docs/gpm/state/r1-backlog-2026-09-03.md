# R1 Backlog — Real-system validation rig (local) — kickoff 2026-09-03

Source: `docs/saas/2026-08-31-readiness-plan.md` §R1 and
`docs/saas/readiness-instructions.md` §R1 (LOCAL variant). Mode: **DELIVERY**
(`mode.md`). Framework: Backlog Builder v5.1/v5.2. Written the evening before the
first run, after merging the 2026-09-02 review fixes (`8ca9140`).

**Why this EPIC first:** the whole build is proven against mocks. Version is 0.9.0
*because* nothing has run against a real WHATS'ON schema or a real IdP. Every other
readiness EPIC (R2 restore drill, R4 pen test, R3 metering) leans on a rig that exists.
The exit criterion carries a **7–14-day unattended clock**, so the day the rig comes up
is the day the clock starts — everything else can run in parallel with it.

## Status — end of day one (2026-09-03)

R1-0 ✓ · R1-1 ✓ (tracer) · R1-2 ✓ · R1-3 ✓ · R1-4 ✓ (tracer, both dialects) · R1-5 ✓
(every checklist row ticked) · R1-6 started (clock day 1; content = the rig goals; the
cockpit still wants Mediagenix's real OKRs) · R1-7 runbook corrections landed, triage
in `docs/saas/readiness/r1-findings.md` (29 findings: 3 fix-now shipped, 1 backlog-high,
rest backlog/accepted). Not on the day-one plan but true: no Docker on this PC, so
Keycloak runs natively on 8081; Oracle `LOCAL` is a real WHATS'ON schema.

## Readiness Decision

**PROCEED.** Inputs are runnable instructions with exact commands, shipped rig assets
(`scripts/local-rig/`: Keycloak compose + realm, PSI test schemas for both dialects),
and a provisioning script verified tonight against today's main (clarity 3,
feasibility 3, completeness 2 — the Oracle side is the least-rehearsed → 8/9).

## Critical Gaps

- **Docker CLI was not on PATH** in tonight's shell. Step 0 tomorrow is a prerequisite
  check, not step 1 of the runbook.
- **Oracle Instant Client / `oracledb`** on this PC has never been exercised by the
  bridge. If the driver does not load, Postgres-only is a legitimate day-one; Oracle
  becomes R1-4b, not a blocker.

## Domain Glossary (delta only)

- **Rig** — the two provisioned instances (`cockpit` :3100, `tenant0` :3101), Keycloak
  (:8080), and one agent process, all on this PC.
- **Discrepancy** — any behaviour that differs between the mocked path (tests) and the
  real system; logged in `docs/saas/readiness/r1-findings.md`, triaged
  *fix-now / backlog / accepted*.
- **Tenant zero** — the client-edition instance that Mediagenix itself uses; the first
  real user of the product.

## Assumptions Ledger

| # | Assumption | Impact | Verified? |
|---|---|---|---|
| 1 | `provision-instance.mjs` produces a bootable client instance on today's main (strict routers, canonical paths, credential rewrap). | High | (verified 2026-09-02 evening) provisioned into scratch: migrations 001–006 applied, `.env` written 0600, client row seeded |
| 2 | Keycloak's realm import (`brokr-realm.json`) still matches the bridge's OIDC expectations (issuer `http://localhost:8080/realms/brokr`, client `brokr-local`, redirect URIs :3100/:3101). | High | ASSUMED — verify at R1-1 by completing one login |
| 3 | The bridge's `allowInsecure` path accepts an `http://` issuer for local Keycloak. | High | ASSUMED — `auth.cjs` comment says so; R1-1 proves it |
| 4 | The PSI test schemas load into your local Postgres/Oracle and the preset queries in `bridge/whatson/templates.cjs` return rows against them. | High | ASSUMED — R1-2 |
| 5 | The `brokr_reader` read-only account can run every preset (SELECT-only, `PSI` schema prefix honoured via `connConfig.schema` since D-6). | Med | ASSUMED — R1-4 |
| 6 | Today's execute-batch rule (non-owners must match stored `liveConfig`) holds for the real UI flows: create-with-live-KR → auto-sync as a **manager** works against a real DB. | Med | ASSUMED — R1-5 is the first real-UI exercise of `bbef495` |
| 7 | The provisioned `.env` has no `BRIDGE_ENCRYPTION_KEY`; credentials encrypt under the `BRIDGE_API_KEY` fallback. Fine for the rig; the docs recommend a dedicated key. | Low | (verified) — R1-0 adds it to the provisioning template so every future instance gets one |

## Backlog

Conventions: branch `readiness/r1-local-rig`, commit per story, `[type](scope): summary`.
Rig state itself (`./local-rig/`) is **not** committed (already gitignored — verify at
R1-0). Findings go to `docs/saas/readiness/r1-findings.md` on the same branch.

---

### EPIC R1 — Prove the product against real systems, locally

- **Objective:** both DB dialects exercised end-to-end through the agent on a read-only
  account; a real OIDC round-trip; Mediagenix's own OKRs live on the cockpit; 7–14
  unattended days; every discrepancy triaged.
- **Tracer Bullet?:** YES — R1-1 + R1-4 together: one real login and one real number
  pushed by the agent. If those two work, the rest is content and patience.
- **DoD:** the plan's exit criteria: run completes; findings triaged; both dialects via
  agent; R1b (Entra) passed; `docs/operations.md` OIDC + agent sections corrected.
- **Risk:** Oracle driver on Windows (see Critical Gaps).
- **Smoke Test Story:** R1-1 (login) and R1-4 (one agent value) — nothing else starts
  until both are green.
- **Runbook:** `readiness-instructions.md` §R1 is the runbook; corrections land there.

---

#### R1-0 — Prerequisites and one provisioning fix · **S · morning**
- **AC:**
  - `docker --version` and `docker info` succeed from the shell you will use.
  - Local Postgres and Oracle reachable; you know the admin credentials for both.
  - `node --version` ≥ 22; `npm run rebuild:node` has been run (the dev bridge must be
    on the system-Node ABI — `electron:build` on 2026-09-02 flipped it once already).
  - `./local-rig/` is gitignored (`git check-ignore local-rig`).
  - `scripts/provision-instance.mjs` emits `BRIDGE_ENCRYPTION_KEY` alongside
    `BRIDGE_API_KEY` (Assumption 7) — commit as `fix(provision): dedicated credential key`.
- **Two Hats:** FEATURE (one-line template change + `provision.test.cjs` assertion).

#### R1-1 — IdP up, one real login on each instance · **S · tracer**
- **Steps:** instructions §R1 steps 1, 3, 4. Set `BRIDGE_PORT` 3100/3101 and
  `BRIDGE_HOST=127.0.0.1` in each `.env` by hand (the script writes 3001/0.0.0.0).
- **AC:**
  - `owner` signs in at :3100 and :3101 → first sign-in becomes owner (check the users
    table / Team page).
  - `member` signs in → role member; a member cannot open Clients (UI) and gets 403 on
    `POST /api/clients` (curl) — this is the first real-IdP exercise of RBAC.
  - Logout and session expiry behave; note the Keycloak claim names actually received
    (`name`/`email`/`preferred_username`) in the findings log.
- **Watch for:** issuer mismatch (`http` vs `https`), redirect-URI mismatch, claim
  mapping in `upsertSsoUser` (`bridge/routes/auth.cjs`).

#### R1-2 — Test schemas loaded, presets return rows · **S**
- **Steps:** instructions step 2, both dialects; create `brokr_reader`.
- **AC:** as owner on tenant0, add each connection on the Clients page (this is the
  first real exercise of `POST /api/connections` → encrypt → `config.json`); "Test
  connection" green; Preview of "Transmissions This Month" returns a number on each.
- **Verify today's work:** bridge startup lines show `Encrypted 0…` / no `unreadable`
  warning; `GET /api/health` (signed in) carries `credentials: { unreadable: 0 }`.

#### R1-3 — Cockpit ↔ tenant channel · **S**
- **Steps:** instructions step 5 (curl for the share token until R6-1).
- **AC:** tenant0 restarts with `BRIDGE_COCKPIT_URL` + `BRIDGE_SHARE_TOKEN`; a KR flipped
  to Shared appears on the cockpit fleet panel within one interval.

#### R1-4 — Agent enrolled, one real value per dialect · **M · tracer**
- **Steps:** instructions step 6. Agent config: both connections under `brokr_reader`,
  bindings using preset SQL. Passwords may be plaintext (local rig) or `enc:v1:` values
  from `encrypt()` with `AGENT_DATA_KEY` — both accepted since `57841e0`; try the
  encrypted form at least once.
- **AC:** `node bridge/agent.cjs run` logs `pushed 2 values`; both KRs on tenant0 show
  the value with `syncStatus: ok`; the Oracle one **and** the Postgres one.
- **R1-4b (if Oracle refuses):** log the driver error verbatim as a finding, proceed
  Postgres-only, and schedule Oracle for when Instant Client is sorted. Not a blocker.

#### R1-5 — Real-UI exercise of the 2026-09-02 fixes · **S**
- As **manager** on tenant0: create a goal with a live KR (real SQL, real connection)
  → auto-sync must succeed immediately (F5; exercises `isStoredQuery` + the
  write-then-sync ordering). Edit its SQL → auto-sync again. "Sync All Live KRs" works.
- As manager: change the SQL in devtools to something not stored and replay the
  execute-batch request → per-result `Not a stored query`.
- Run the Setup Wizard as manager (Help → Set up): goal step appears, connection step
  does not; create a goal whose query fails → the step still shows "created", one goal
  in the store (F6).
- Stop tenant0, set a *different* `BRIDGE_ENCRYPTION_KEY`, start → startup warns
  `N stored connection password(s) cannot be read`; Dashboard shows the warning (F4);
  restore the key → warning gone, nothing on disk changed (F2).
- **AC:** each bullet observed and ticked in the findings log; anything off is a
  fix-now finding.

#### R1-6 — Content and the clock · **S then 7–14 days**
- Cockpit: Mediagenix's real OKRs (yours). tenant0: goals whose KR ids match the agent
  bindings; one Shared. Bridges + Keycloak as Windows startup tasks (Task Scheduler or
  `pm2-windows-service` — record which in the findings log).
- **AC:** day 1 date written at the top of `r1-findings.md`; a daily one-line entry
  (values still arriving? staleness banner honest? backups pairing in `backups/`?
  `bridge-process.log` size?).

#### R1-7 — Triage and runbook corrections · **S · at close**
- Every finding *fix-now / backlog / accepted-with-date*. Fix-now items ship on this
  branch as their own commits. `docs/operations.md` OIDC and agent sections corrected
  from what the rig taught; `readiness-instructions.md` §R1 corrected where a step was
  wrong.

#### R1b — Corporate Entra spot-check · **half day · later, before any client demo**
- Per instructions. Not on tomorrow's path.

---

## Sequencing & effort

```
R1-0 (S) → R1-1 (S, tracer) → R1-2 (S) → R1-4 (M, tracer) → R1-5 (S) → R1-6 (S + clock) → R1-7
                              └→ R1-3 (S) — any time after R1-1, needed before R1-6's Shared KR
```

Tomorrow's realistic target: **R1-0 through R1-4** (both tracers green) and R1-5 if
the afternoon allows. R1-6 starts the clock the same day the agent pushes its first
real value. While the clock runs: D-3's decision spike and R6-1 (admin UIs) — both
were named in the 2026-09-02 close-out as the parallel work.

## Roadmap updates this backlog requires

- `docs/saas/readiness-instructions.md` §R1: add step 0 (prerequisite check incl.
  Docker on PATH), the port/host edit reminder, and the R1-5 checklist as a permanent
  "verify the last review's fixes on the rig" step.
- `NEXT-SESSION.md`: pickup block points here.
