# R7 Backlog — Release engineering — kickoff 2026-09-04

Source: `docs/saas/2026-08-31-readiness-plan.md` §R7 and `docs/saas/readiness-instructions.md`
§R7. Mode: **DELIVERY** (`mode.md`). Framework: Backlog Builder v5.2.

**Why now:** 0.9.2 carries D-3, R6-1..R6-5 and migrations 007–010. Until now every
release was a hand-built Windows installer from one PC (0.9.1 shipped on 2026-09-03 with a
NODE_MODULE_VERSION mismatch caught only because someone ran it). The cloud instances on the
rig are hand-built bundles started from the working tree. One tag must produce every artifact.

## Readiness Decision

**PROCEED.** Everything the workflow needs exists: `electron:build*` (with the forced
better-sqlite3 rebuild), `check-edition-bundle.mjs`, the bridge test runner, `/api/health`
and `__APP_VERSION__` already surface versions from `package.json`, FF-5 fixtures have a
replayer. Missing: a release workflow, an instance image that carries the app bundle (the
bridge Dockerfile ships the bridge alone), an agent bundle, the desktop update signal, and
the fixture capture. Clarity 3, feasibility 3, completeness 3 → 9/9.

## Assumptions Ledger

| # | Assumption | Impact | Verified? |
|---|---|---|---|
| 1 | `GITHUB_TOKEN` with `packages: write` can push to `ghcr.io/<owner>/…` on this public repo. | High | ASSUMED — verified by the first tagged run |
| 2 | `electron-builder --linux` on `ubuntu-latest` and `--win` on `windows-latest` need no extra system packages for AppImage/deb/NSIS. | Med | ASSUMED — first run |
| 3 | The tag is the version: `vX.Y.Z` must equal `package.json`/`bridge/package.json` version, or the run fails. Releases come from a version-bump commit, not from CI rewriting files. | High | (decision) |
| 4 | better-sqlite3 has prebuilt binaries for `node:22-slim` (glibc x64), so the image build needs no toolchain. | Med | ASSUMED — first run |
| 5 | The desktop update check may call `api.github.com` from the renderer (public repo, no token, once a day). | Low | (verified by design) |

## Backlog

Conventions: branch `feature/r7-release-engineering`, commit per story, tests first where a
test can exist locally (the workflow itself is verified by the tagged run), suites + lint +
build green before each commit, merge `--no-ff`.

### EPIC R7 — One tag, three artifacts

#### R7-1 — Release workflow, instance image, agent bundle · **M** · **DONE 2026-09-04** (workflow verified by the v0.9.2 run — see R7-4)
- `.github/workflows/release.yml` on `v*`: `verify` (tag == version, tsc, lint, vitest,
  bridge tests, build, FF-1 for all three editions) → `desktop` (windows + linux
  installers), `image` (client + cockpit images to GHCR), `agent` (tarball) → `release`
  (GitHub Release with generated notes, artifacts attached, image names in the notes).
- `broadcastokr/Dockerfile` (multi-stage: app bundle per edition + bridge; `BRIDGE_APP_DIR`
  set; `.dockerignore`). `docker-compose.yml` points at it.
- `scripts/build-agent-bundle.mjs`: `bridge/agent.cjs`, `agentCore.cjs`, `protocol.cjs`,
  `whatson/`, `utils/crypto.cjs`, `utils/atomicWrite.cjs` + a generated manifest with the
  DB drivers as optional dependencies + a README. Unit test: the bundle's require graph is
  closed (every relative require inside the staging dir resolves).
- **AC:** locally, `node scripts/build-agent-bundle.mjs` yields a tarball whose `agent.cjs
  --help` runs from a clean directory after `npm install`; the tag-check step refuses a
  mismatched tag; the workflow lints (`actionlint` not available here — reviewed by eye).

#### R7-2 — Desktop update signal · **S** · **DONE 2026-09-04**
- `src/utils/updates.ts`: `isNewerVersion`, `fetchLatestRelease` (GitHub latest release →
  tag), `shouldCheck` (once per 24 h, last check in localStorage). App: on the desktop
  edition only, after the first healthy poll, a passive toast "BroadcastOKR X.Y.Z is
  available" with the release URL; the System Health panel shows the line too.
- **AC:** unit tests for the version compare and the interval; no network in tests.

#### R7-3 — FF-5 fixture capture · **S** · **DONE 2026-09-04** (verified 7/7 against v1, nothing captured)
- `scripts/capture-protocol-fixtures.mjs`: boots the bridge from the working tree, replays
  the newest fixture set, and — when `PROTOCOL_VERSION` has no fixture directory yet —
  writes `bridge/__tests__/fixtures/protocol-v<N>/requests.json` from the previous set
  (every request re-verified against the booted server). The release workflow runs it and
  opens a PR when files appear. No-op today (v1 exists).
- **AC:** running it against v1 verifies all fixtures and writes nothing.

#### R7-4 — Cut 0.9.2 · **XS**
- Bump both `package.json` files, tag `v0.9.2`, push the tag, watch the workflow, download
  the Windows installer on this PC and install over 0.9.1 (the `config.json` import path),
  pull the cockpit image name into `docs/operations.md`.
- **Exit for R7:** the tagged run produces installers + images + agent bundle + notes with no
  manual step.
