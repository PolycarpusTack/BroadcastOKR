# Review remediation backlog

Date: 2026-09-07. Mode: **DELIVERY**. Input: [solution plan](plan.md), baseline `de815e6` / `0.9.2`. This is a planning artifact; implementation has not started.

## Readiness decision

**PROCEED with the ready queue; dependent implementation remains HOLD at the named gates.** Design score: clarity 3, feasibility 2, completeness 3 = **8/9**. Business context, architecture, data models, interfaces, and user journeys are present. High risks have explicit mitigations and release gates. The feasibility deduction reflects unverified SQL/container infrastructure and three bounded design choices. Independent validation is a separate pass in [validation.md](validation.md); this generation does not self-certify acceptance.

## Critical gaps

- A-2-T1 must select a SQL-aware validator against both repository dialect corpora and specify the supported database enforcement envelope. Both real drivers remain required for release evidence.
- B-3-T1 must resolve legacy/versionless check-in compatibility, same-goal queue ordering, response shape, and idempotency retention before implementation.
- B-4-T1 must compare deletion markers/cursors with authoritative inventories/snapshots and choose the smallest correct protocol before server/client implementation.
- A-4 cloud tests need isolated authenticated client/cockpit images and a test IdP. Existing desktop/dev E2E configuration is insufficient. Infrastructure absence blocks the corresponding acceptance, not independent planning or implementation.

## Domain glossary

The glossary in [plan.md, Architecture and domain](plan.md#architecture-and-domain) is authoritative. Owner, Manager, Member, Goal, Key result (KR), Check-in, Entity version, Deletion marker, and Pull gate retain those meanings. Manual KR measured values and live KR annotation-only check-ins are distinct. `kpis` means the legacy synchronized slice; `kpi_definitions` means the separate dashboard store. “Tombstone” is a synonym for deletion marker only if B-4 selects that design. “Operation ID” is the stable retry identity of one check-in, not an entity version. Use these terms consistently in code and contracts.

## Assumptions ledger and risk analysis

| ID | Assumption / risk | Mitigation and recheck gate |
|---|---|---|
| H1 | Existing role policy and desktop trust model remain authoritative; changing them is high impact | Preserve permitted controls and session-derived identity; A-1/A-3 review |
| H2 | Oracle/PostgreSQL test services, Docker, and test IdP availability is unverified | Use disposable fixtures when available; label outstanding evidence explicitly; A-2/A-4 acceptance |
| H3 | New check-in clients may coexist with versionless full-goal writers | Resolve and test support matrix/minimum safe client in B-3-T1; never silently raise protocol floor |
| H4 | Proposed 24-hour operation retention and 30-day deletion retention are adequate | Confirm replay/restore envelope and metadata minimization in owning ADR; surface changed retention commitments before implementation |
| H5 | Cursor design might cost more than an inventory/snapshot solution | B-4-T1 compares correctness, complexity, bounded load, and rollout; low confidence triggers refinement |
| H6 | Old binaries may reopen security defects or mishandle new data | Keep protection enabled; disable affected operation or use patched build; retain additive schema on rollback |
| M1 | New transaction/sync work could grow into a rewrite | Scoped components and countable task limits below; record unrelated aggregates as follow-ups |
| M2 | Current green tests may preserve the defect | Replace history-only and POST+PUT expectations; assertions include stored rows and independent client state |

STRIDE: **S** session or check-in attribution spoofing → authenticated principal and existing local trust contract; **T** unauthorized edits/SQL writes/partial aggregates → C1/C2/C4/C5; **R** ambiguous retries and mutation attribution → operation IDs and minimal structured outcomes; **I** SQL, credentials, notes, and user data in logs/markers → redaction/minimal metadata/synthetic fixtures; **D** expensive queries/poll replay/unbounded history → timeout, coherent cursors/reset, bounded retention; **E** encoded-ID roles, broad task fields, database grants → server-side policy plus least privilege and database enforcement.

Compliance: **no regime specified**; this is not a certification. User identities, history, and notes are sensitive application data; tests use synthetic people/data. New operation records contain IDs, request digest, and minimal response metadata; deletion markers contain kind/ID/sequence/time only. Existing business-data retention is unchanged. No tokens, credentials, raw SQL, or notes enter logs or evidence artifacts.

## Shared execution contract

All story/task fields below inherit this section; a local field overrides it. This avoids repeating the full framework template.

- **DoR:** persona, benefit, Gherkin success/alternative/error cases, referenced C-contract, dependencies, synthetic fixtures, sensitivity, and relative size must be known. READY means a task can be pulled after its gate; HOLD names missing design/upstream evidence. A HOLD delivery story can contain an independently READY preparatory task. INVEST is shown per story; I means independently valuable, not dependency-free.
- **Flow:** WIP 1 implementation; independent specialist review is allowed. Preferred queue: A-1 → A-2 → A-3 → A-4 → B-1 → B-2 → B-3 → B-4 → B-5 → B-6. This is scheduling preference; only explicit Pull gates/Unblocks are hard dependencies. After documenting an unavailable external gate, pull another READY task. No calendar-day estimates; forecast later using measured cycle-time percentiles.
- **Task budget:** one logical component, expected total code/tests about 400 lines or less, at most 15 meaningful cases, output ≤15k tokens, project context ≤3k tokens. Scope excludes generated lockfile lines. If a task exceeds a countable bound or touches more than three complex modules, split it before pulling and retain dependencies. Do not split cohesive logic solely to inflate task counts.
- **Hat and TDD:** FEATURE means corrective behavior change; PREPARATORY means contracts/fixtures/evidence. Each FEATURE task starts with failing regression + permitted control, implements the smallest fix, and ends with verification. Any structural extraction is a separate PREPARATORY/REFACTORING task, not mixed into that change. PREPARATORY starts with an explicit unmet acceptance checklist or failing contract probe, supplies its artifact, then verifies it; do not invent unit tests for prose.
- **Interfaces/handoff:** task references to C1–C7 mean [plan.md, Proposed solution contracts](plan.md#proposed-solution-contracts). Each changed interface produces a small versioned `contracts/<component>.md` snapshot: API/types/error shapes, dependencies, glossary. Reuse `docs/gpm/state/architecture-memory.md` if present and applicable; otherwise initialize local `architecture-memory.md` in A-1, then keep the local remediation summary current. No interface change is considered accepted merely because it appears in this backlog.
- **Abstraction:** reuse existing auth/SQLite/driver/queue boundaries. First occurrence stays inline; second is recorded; third equivalent pattern within one bounded context is reviewed for extraction in a separately gated preparation task. No blanket repository refactor.
- **Global gates:** failing-before/passing-after evidence; all changed story cases; relevant unit/API/contract tests; lint/build; new logic ≥80% unit coverage where measured; no secrets; integration note in plain language; snapshots and minimal runbook updates; no new undocumented debt. External integrations get an owned-seam mock and real contract evidence. Existing full-suite counts from the prior review have not been rerun in planning and do not close findings.
- **Rollout/flags:** security checks default ON and cannot be bypassed by any flag. For role/task/query protection, emergency containment disables the affected write/query operation. Proposed `checkin_command_enabled` defaults OFF until coordinated contract/compatibility acceptance; OFF rejects affected check-ins rather than enabling the broken write sequence. Proposed `sync_reconciliation_enabled` defaults OFF pending server/client acceptance; OFF uses a verified coherent snapshot fallback, and cannot count as closure until that fallback is demonstrated. Exact configuration names are finalized in owning ADRs. No broad flag service is required.
- **Migration/rollback:** additive migrations are repeatable and tested on populated fixtures; retain added schema on binary rollback. Feature enablement is gated on compatible clients. Restore tests use disposable copies and report data-loss windows; no automatic destructive down-migration. A rollback may not restore an authorization or SQL bypass.
- **Writes/idempotency:** role/task assignment to the same allowed state converges; existing versions detect conflicting goal/task updates with 409 and do not silently replay against newer versions. Creates reuse client-generated IDs and an explicit duplicate/conflict result. Deletes are repeatable and cannot create duplicate effects. Check-ins require the B-3 operation contract; legacy unkeyed requests are outside its deduplication guarantee.
- **Operations:** reuse request IDs and existing logging/measurement hooks; add only needed outcome, latency, timeout, replay/reset/conflict signals. Correlate client/bridge/driver boundaries where these flows cross them; no unrelated tracing platform rollout. Runbooks cover symptoms, safe diagnostics, expected signals, containment, recovery, and rollback. Thresholds below are proposed validation objectives, not measured production SLOs.
- **Routing:** judgment → reasoning-capable agent; implementation → execution-capable agent; acceptance → independent reviewer. This run uses inherited models, not claimed execution of Claude model names. Task confidence 3=high, 2=medium, 1=low; 1 requires a refinement task. Parallelisation: independent review only; coupled state mutation remains serial.
- **Debt:** TD Created defaults `none`; discovered shortcuts require ID, artifact, type/cause, principal (relative size), recurring interest, compounding status, owner, and servicing decision before acceptance. Security/data-integrity findings cannot be accepted as debt. Existing Windows permission assertion and unrelated aggregate patterns remain visible follow-ups, not excuses to skip changed Linux/transaction checks.
- **Evidence location:** local `evidence.md`, `runbook.md`, `contracts/`, and short `adr/` records are planned outputs during execution; record commit, command, platform, pass/fail, and artifact path. Branch convention if later authorized: `feature/<STORY-ID>-<description>`; commit convention: `fix(<scope>): <summary>`. This planning request authorizes no commits, external messages, publication, or deployment.

## Epic A — Close authorization/query boundaries and prove cloud sign-in

Mode DELIVERY; **Tracer bullet YES**: the existing session → HTTP middleware → user route → SQLite role-change journey, with owner/manager controls, is A-1; clean cloud/browser packaging is proven in A-4. Value: zero forbidden writes and usable packaged cloud authentication. Glossary: Owner/Manager/Member, task, session, query. Architecture memory: shared contract above. ADRs: ADR-A1 permission identities/field policy; ADR-A2 SQL envelope; ADR-A4 runtime authentication packaging. Runbook: `runbook.md#epic-a`; smoke stories A-1/A-4.

Epic DoD additions: (1) F1/F2/F5 regressions reject forbidden operations with unchanged persisted data; (2) clean client and cockpit images complete OIDC sign-in/out/restart flow; (3) independent security/test review accepts evidence and phase summary updates architecture memory. Validation objectives: permission service – forbidden writes = 0 over the full case matrix; SQL service – prohibited fixture writes = 0 over the driver corpus and timeout/cancellation bounded by existing 15-second policy. Risks/mitigations: H1/H2/H6 and M2 above. External evidence can remain pending while independent ready work progresses.

### A-1 — Use canonical target identity for role permissions (F1)

As an Owner, I want role changes authorized for the actual stored user so that Managers cannot promote themselves through URL variations. Value 3; priority H: direct privilege escalation. Size S; confidence 3; **DoR READY**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interfaces C1; dependencies local existing modules/isolated SQLite, availability confirmed by review but recheck on pull. Data: synthetic owner/manager, target ID 0/2 and unknown IDs. Debt: no shortcut accepted; unrelated role-policy changes excluded. Idempotency: repeated allowed same-role assignment converges; repeated forbidden request makes zero writes.

- Given real owner/manager sessions and target 2, when the target appears as `2`, `%32`, path case/trailing-slash variants, or equivalent accepted encoding, then only the Owner can change its role and every denied request leaves SQLite unchanged.
- Given invalid/double-encoded/non-numeric/unknown targets, when role editing is attempted, then the request fails closed with the documented client error; given an allowed manager profile edit or valid ID 0, when submitted, then existing permitted behavior remains.
- **A-1-T1 [FEATURE; execution; S; confidence 3]** Fix the user-role boundary with real session/RBAC/router/SQLite regression and permitted-path control; deliver tests → scoped route/policy change → ADR-A1 identity rule, permission snapshot, integration note and initial architecture memory. Pull gate: confirm current route decoding, valid-ID contract, baseline, and isolated fixture. Unblocks: A-4-T2 session smoke contract, B-6-T1. Flag/rollback: always-on policy; disable role changes if emergency containment is required.

### A-2 — Enforce the supported read-only SQL envelope (F2)

As an Operator, I want validated queries and database enforcement to agree so that customer data cannot be modified through the query runner. Value 3; priority H: destructive-query bypass. Size L; confidence 2 after refinement; **DoR HOLD for implementation until A-2-T1**; INVEST I✓ N✓ V✓ E✗→refinement S✓ T✓. Interface C2; external dependencies Oracle/PostgreSQL disposable test services, availability unverified. Data: repository positive SQL corpus plus synthetic destructive/quote/comment cases and disposable tables/functions. Debt: parser incompatibility cannot be silently waived; unsupported syntax must reject actionably. Idempotency: read operations have no mutation retry semantics; timeout must not reuse dirty transactions.

- Given a supported Oracle/PostgreSQL SELECT with literals/comments/quoted identifiers, when executed, then the validated original SQL produces the expected read result; invalid/unsupported syntax fails before any driver call.
- Given the reported `SELECT '--'; DELETE FROM review_only` payload, dollar/alternative quoting, multiple statements, SELECT INTO, locking, or prohibited function behavior, when exercised within the documented envelope, then validation/enforcement refuses the prohibited operation and disposable fixture state is unchanged.
- Given cancellation/error followed by pool reuse, when another query borrows the connection, then rollback/session cleanup has completed and no unprotected fallback executes; grants/side-effect limits remain explicit.
- **A-2-T1 [PREPARATORY; reasoning; S; confidence 2; READY]** Capture the unmet SQL corpus checklist; select parser/tokenizer and allowed syntax, driver enforcement lifecycle, grants envelope, and negative cases in ADR-A2/SQL snapshot. Pull gate: read C2, both dialect templates, current timeouts, shared agent entrypoints; confirm test-service availability separately. Unblocks: A-2-T2/T3/T4. Flag: execution may be disabled; validator never bypassed. Rollback: preserve existing code while contract is reviewed.
- **A-2-T2 [FEATURE; execution; M; confidence 2]** Implement the selected single-statement validator as one core component; tests → validator → SQL snapshot/corpus evidence. Pull gate: accepted A-2-T1 contract and bounded corpus. Unblocks: A-2-T5. Quality: exact SQL forwarded equals validated input; unsupported syntax fails closed. Flag/rollback: query execution OFF for containment; no unsafe old validator fallback.
- **A-2-T3 [FEATURE; execution; M; confidence 2]** Implement PostgreSQL same-connection read-only lifecycle with owned-seam mock and real disposable-driver contract tests; tests → adapter → timeout/pool cleanup evidence and runbook. Pull gate: A-2-T1 lifecycle accepted; real service required for acceptance, not coding. Unblocks: A-2-T5. Flag/rollback: disable PostgreSQL query execution; preserve least-privilege account requirement.
- **A-2-T4 [FEATURE; execution; M; confidence 2]** Implement Oracle read-only lifecycle with owned-seam mock and real disposable-driver contracts including alternative quoting, rollback, cancellation, and pool reuse; tests → adapter → enforcement-envelope evidence. Pull gate: A-2-T1 accepted; Oracle service required for acceptance. Unblocks: A-2-T5. Flag/rollback: disable Oracle query execution; no universal function-safety claim.
- **A-2-T5 [PREPARATORY; independent review; S; confidence 2]** Prove every bridge/preview/batch/KPI/scheduled/agent caller reaches the protected core and packaged agent contains it; failing entrypoint/bundle checks → minimal integration wiring if needed in a separately FEATURE-hatted follow-up → evidence and operator runbook. Pull gate: A-2-T2/T3/T4 implementation contracts; real-driver results needed to accept F2. Unblocks: B-6-T1. Flag/rollback: disable affected entrypoint if evidence fails; never use an unprotected runner.

### A-3 — Permit only member task status/subtask completion writes (F5)

As a Member, I want permitted task progress updates to succeed so that I can report work without gaining structural edit or assignment rights. Value 3; priority H: current authorization bypass. Size M; confidence 3; **DoR READY**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interface C4; dependencies local route/SQLite/current full DTO, recheck on pull. Data: synthetic full/restricted payloads, existing subtasks, stale version, missing task. Debt: no broad permissions rewrite. Idempotency: versioned same-state writes use existing conflict contract; absent fields do not erase stored state.

- Given a Member's current full task DTO, when only status or `done` of an existing subtask changes and structural fields are equal, then it succeeds; a supported restricted payload preserves omitted fields.
- Given changed title/assignee/priority, altered subtask text/order/membership, or mixed allowed and forbidden edits, when a Member submits them, then 403 occurs and every row/version remains unchanged.
- Given malformed payload, unknown task, or stale version, when submitted, then documented 400/404/409 applies without partial effects; Owners/Managers retain allowed edits and assignments.
- **A-3-T1 [FEATURE; execution; M; confidence 3]** Add field-difference authorization against one consistent stored/versioned task and preserve full/restricted DTO behavior; tests → scoped task route/policy change → ADR-A1 field extension/task snapshot. Pull gate: C4/current DTO and role policy confirmed; no dependency on completing A-2. Unblocks: B-2-T1, B-6-T1. Quality: persisted-state matrix, not mocked permission booleans. Flag/rollback: field policy always ON; disable task editing for containment.

### A-4 — Ship reproducible cloud SSO runtimes (F3)

As a Cloud Operator, I want fresh client and cockpit images to contain their SSO dependencies so that users can sign in after deployment or restart. Value 3; priority H: clean cloud sign-in unavailable. Size M; confidence 2; **DoR READY for T1; T2/T3 HOLD on their gates**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interface C3; dependencies Docker/test IdP/Linux CI, availability unverified. Data: synthetic isolated OIDC clients/users and ephemeral secrets. Debt: no image acceptance based only on build/health; no provider migration. Idempotency: reproducible installs/builds and existing OIDC state/nonce/single-use callback behavior.

- Given an empty build context dependency cache and no host/root `node_modules`, when each image is built and started, then bridge production imports including `openid-client` resolve from the declared reproducible dependency graph.
- Given an isolated client or cockpit image and test IdP, when login/callback/me/logout/restart are exercised, then the authorized identity persists as designed and unauthenticated/invalid-state flows are refused.
- **A-4-T1 [FEATURE; execution; S; confidence 3]** Add bridge production dependency/lockfile and consistent runtime installs in both Dockerfiles; failing isolated import/build contract → manifests/package configuration → deployment/env documentation and runtime snapshot. Pull gate: C3 and current bundle/install assumptions. Unblocks: A-4-T2. Flag/rollback: existing cloud mode; route unavailable if auth runtime fails, no anonymous bypass; pin patched known-good image for rollback.
- **A-4-T2 [PREPARATORY; execution; M; confidence 2]** Build the isolated cloud OIDC browser fixture and prove both image journeys, including owner/member identities and restart; failing journey checks → fixture → evidence/test-identity runbook. Pull gate: A-4-T1, A-1-T1 session permission snapshot, available Docker/IdP. Unblocks: A-4-T3, B-6-T1. Quality: authenticated cloud mode, not existing desktop/dev E2E. Rollback: remove only disposable test state; retain failed evidence.
- **A-4-T3 [FEATURE; execution; S; confidence 3]** Gate PR and release publication workflows on fresh-image SSO contracts; failing workflow gate check → CI/release configuration → evidence links. Pull gate: stable passing A-4-T2 command/artifacts. Unblocks: B-6-T1; END OF EPIC A implementation sequence. Flag/rollback: failing gate blocks publication; no publish/deploy is performed by this task without later authorization.

## Epic B — Make accepted mutations durable across clients

Mode DELIVERY; Tracer bullet NO. Value: one accepted measurement persists once, no partial aggregate writes, deleted data converges across supported clients. Glossary: Goal/KR, Check-in, Entity version, Operation ID, Deletion marker. Architecture memory: A phase summary plus local remediation memory; independent root B-1 can run if an A external evidence gate is unavailable. ADRs: ADR-B1 aggregate transactions; ADR-B3 check-in compatibility/idempotency; ADR-B4 reconciliation protocol. Runbook: `runbook.md#epic-b`; smoke story B-6.

Epic DoD additions: (1) F7 failure injection restores all rows/version and F4 retry yields one history result; (2) F6 supported collections converge and cannot resurrect deleted records; (3) cloud two-session E2E, migration/compatibility/rollback evidence, and independent review pass. Proposed validation objectives: check-in/sync service – observer convergence ≤10 seconds over isolated two-session scenarios at current 5-second polling; mutation service – partial writes = 0 over injected failures and p95 <250 ms across 100 requests on a documented 1,000-goal/1,000-task fixture. Establish baseline before committing the latency target. Risks H3–H6/M1/M2 have the gates above; no production SLO claim.

### B-1 — Make goal aggregate writes atomic (F7)

As a Goal Owner, I want an invalid or failed goal update to leave the complete previous goal intact so that visible errors do not corrupt progress or versions. Value 3; priority H: persistence foundation for F4. Size M; confidence 3; **DoR READY**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interface C5; dependency local SQLite/migration fixtures. Data: synthetic parent/KRs, invalid nested values, foreign-goal KR IDs, stale versions, injected late failure. Debt: other aggregate families excluded and recorded if analogous. Idempotency: existing client-generated create ID and explicit duplicate result; updates retain 409 conflict semantics.

- Given invalid nested input or an injected failure after parent mutation, when create/update runs, then all parent/child/history/version/timestamp rows equal the pre-request state and the documented error is returned.
- Given a KR owned by another goal or a stale entity version, when update runs, then it cannot alter either aggregate; valid create/update commits every intended row once.
- **B-1-T1 [FEATURE; execution; M; confidence 3]** Add prevalidation, KR ownership checking, and synchronous goal aggregate transaction boundaries; tests → route mutation → ADR-B1/goal snapshot/failure evidence. Pull gate: C5, current version/create duplicate contract, and SQLite rollback behavior verified. Unblocks: B-2-T1 transaction convention, B-3-T1/T2, B-4-T3, B-6-T1. Flag/rollback: atomicity always ON; disable goal writes or use patched build if needed; no network await inside transaction.

### B-2 — Make task aggregate writes atomic (F7)

As a Task Owner, I want nested task failures to preserve the original task so that its version, fields, and subtasks stay consistent. Value 3; priority H: known analogous partial-write pattern. Size M; confidence 3; **DoR HOLD until A-3-T1/B-1-T1 contracts**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interface C4/C5; dependency local SQLite. Data: synthetic tasks/subtasks, malformed/reordered input, stale versions, failure injection. Debt: no generic repository transaction abstraction unless existing repetition justifies separate preparation. Idempotency: existing create IDs and version-conflict semantics.

- Given an invalid subtask or injected late failure, when an authorized task write runs, then parent/subtasks/version/timestamps remain unchanged; given a valid update, when it commits, then every intended row changes together.
- Given a Member's mixed forbidden fields or a stale task version, when update runs inside the transaction boundary, then A-3 denial/409 remains intact with zero writes.
- **B-2-T1 [FEATURE; execution; M; confidence 3]** Apply validated synchronous task transaction boundaries with nested failure tests; tests → task route → updated task snapshot/ADR-B1 evidence. Pull gate: A-3-T1 field policy and B-1-T1 transaction convention accepted. Unblocks: B-4-T4, B-6-T1. Flag/rollback: atomicity and field restrictions always ON; disable task writes for containment.

### B-3 — Persist and display a member check-in exactly once within its retry window (F4)

As a Member, I want one authorized check-in to update the current manual KR value and history so that my progress survives reload and another session sees it. Value 3; priority H: primary workflow currently loses authoritative value. Size L; confidence 2 after refinement; **DoR HOLD until B-3-T1**; INVEST I✓ N✓ V✓ E✗→refinement S✓ T✓. Interface C6/C5; dependencies B-1 and local API/store fixtures; cloud journey uses A-4-T2. Data: manual/live KRs, forged attribution, wrong goal/KR, NaN/infinite values, response loss, duplicate/conflicting operation keys, expired key, stale/legacy client DTOs. Debt: no silent compatibility break. Idempotency: principal/goal/operation ID plus digest, atomically stored bounded replay record; legacy unkeyed requests explicitly excluded.

- Given a Member and manual KR, when a check-in succeeds, then current value/history/parent version change atomically and the response refreshes the client without structural PUT; live KR measurement remains unchanged while permitted annotation/history records.
- Given response loss, when the same operation is retried within retention, then one history/value result is returned; conflicting key reuse, invalid value, wrong association, or spoofed cloud attribution cannot create an extra/misattributed mutation.
- Given stale versioned or supported versionless writers and same-goal queued operations, when they race with a check-in, then the accepted value cannot be rolled back; a previously captured body is never paired with a newly fetched version, and an idempotent replay response cannot regress a newer cached goal. The tested legacy/new-client matrix and minimum safe client govern any unsupported case.
- **B-3-T1 [PREPARATORY; reasoning; S; confidence 2; READY after B-1 contract]** Resolve additive response/errors, operation retention/replay, legacy writer strategy, minimum safe client, and same-goal ordering in ADR-B3/check-in snapshot. Start with unmet compatibility checklist including history-only bridge tests and frontend POST+PUT expectations. Pull gate: C6, B-1-T1 version contract, real queue/versionless writer inventory. Unblocks: B-3-T2/T3/T4. Flag/rollback: finalize coordinated enablement; floor/role/retention changes require explicit scope decision before dependent code.
- **B-3-T2 [FEATURE; execution; M; confidence 2]** Add minimal persisted operation-record migration/pruning/replay storage component; failing migration/restart/expiry/conflicting-key tests → storage/migration → schema snapshot and restore evidence. Pull gate: accepted B-3-T1 scope/key/digest/retention contract. Unblocks: B-3-T3. Flag: check-in command OFF pending safe rollout. Rollback: retain additive table; no business-history deletion.
- **B-3-T3 [FEATURE; execution; M; confidence 2]** Implement atomic check-in command with manual/live semantics, trusted attribution, operation replay, and parent signal/version; tests → goal check-in route → response snapshot and persisted evidence. Replace history-only `checkin-propagation` expectations. Pull gate: B-1-T1/B-3-T1/T2 contracts and accepted legacy-writer guard. Unblocks: B-3-T4. Flag/rollback: command containment rejects check-in, never routes back to the unsafe POST+PUT behavior.
- **B-3-T4 [FEATURE; execution; M; confidence 2]** Consume authoritative response, preserve stable operation ID across retries, order same-goal check-in/PUT queue entries, and recalculate presentation; failing store/queue tests → check-in client workflow → client snapshot. Replace `src/store/__tests__/checkInBridge` POST+PUT expectation. Pull gate: B-3-T3 response/error contract and B-3-T1 compatibility decision. Unblocks: B-6-T1. Quality: retry/replay cannot regress a newer cache; no stale captured body with fresh version; no structural PUT after check-in. Flag/rollback: coordinated safe-client enablement per ADR-B3.

### B-4 — Supply authoritative deletion reconciliation (F6)

As an Operator, I want deleted records represented in synchronization so that connected caches can remove every supported entity safely. Value 2; priority M: persistent stale data/correctness. Size L; confidence 2 after refinement; **DoR HOLD for delivery until B-4-T1**; INVEST I✓ N✓ V✓ E✗→refinement S✓ T✓. Interface C7/C5; dependencies local SQLite and versioned sync fixtures. Data: every synchronized collection with deletion, parent cascades/SET NULL, same timestamps, ID recreation, old cursor, restart/restore. Debt: no speculative event framework. Idempotency: repeat delete/replay converges, sequence/snapshot ordering prevents false resurrection.

- Given deletion of any supported synchronized top-level entity, when changes are read, then the chosen protocol provides enough coherent authority to remove it and update affected parent DTOs; a deleted entity cannot be recreated by stale update.
- Given duplicate/same-time/reordered delivery, deletion then ID recreation, retention expiry, initial snapshot interruption, or restore/restart, when a client resumes, then protocol rules produce the correct state or an explicit coherent reset with bounded work. Given timestamp/protocol-v1 clients that ignore additive fields, when support is claimed, then a tested full-resync fallback actually converges; otherwise an explicit minimum-safe-version decision precedes implementation.
- **B-4-T1 [PREPARATORY; reasoning; S; confidence 2; READY]** Compare markers/cursors with authoritative inventories/full snapshots using the same correctness/load checklist; inventory every deletion/cascade and choose minimal design in ADR-B4/sync snapshot. Define watermark/ordering/reset/retention/restore/flag contract, measured fixture budget, and a tested legacy-client full-resync fallback or explicit minimum-safe-version decision. Pull gate: C7 and actual sync/delete/client inventory; do not assume proposal approval or that old clients consume new fields. Unblocks: B-4-T2/T3/T4/T5, B-5-T1/T2. Rollback: preserve security controls and verified snapshot fallback.
- **B-4-T2 [FEATURE; execution; M; confidence 2]** Implement selected sync read/storage protocol component, including additive migration if selected, coherent full/delta boundary, expiry/reset, and pruning; failing protocol/migration probes → core sync implementation → server snapshot/restore evidence. Pull gate: accepted B-4-T1 contract and bounded component output; split storage/read subcomponents first if >400 expected code/test lines. Unblocks: B-4-T3/T4/T5, B-5-T1. Flag: reconciliation OFF until accepted; fallback must pass snapshot tests. Rollback: retain schema/sequence state.
- **B-4-T3 [FEATURE; execution; S; confidence 3]** Make goal deletion and its chosen reconciliation effect atomic, including nested/cascade invalidation; tests → goal delete path → deletion contract evidence. Pull gate: B-1-T1 and B-4-T2 contracts. Unblocks: B-6-T1. Flag/rollback: selected sync rollout contract; no stale-update recreation; disable affected delete if coherent delivery unavailable.
- **B-4-T4 [FEATURE; execution; S; confidence 3]** Make task deletion/reconciliation effect atomic and reject stale task update resurrection; tests → task delete path → deletion contract evidence. Pull gate: B-2-T1 and B-4-T2 contracts. Unblocks: B-6-T1. Flag/rollback: selected sync rollout contract; disable affected delete if coherent delivery unavailable.
- **B-4-T5 [FEATURE; execution; M; confidence 2]** Connect existing reference-entity deletions (clients/users/teams/goal templates/legacy KPI rows where supported) and affected-parent invalidation to the accepted reconciliation contract; data-driven persisted regressions → existing CRUD delete wiring → completed collection coverage inventory. Pull gate: B-4-T1/T2 inventory/helper contract; split by entity before pull if >400 code/test lines or >15 cases. Unblocks: B-6-T1. Flag/rollback: selected sync contract; preserve current authorization and cockpit allowlist, no new deletion endpoints.

### B-5 — Apply synchronization without stale or resurrected records (F6)

As a Connected User, I want my cache to converge with authoritative deletions so that stale polls or pending edits cannot restore removed data. Value 2; priority M: completes F6 across clients. Size M; confidence 2; **DoR HOLD until B-4-T1/T2 contract**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Interface C7; dependencies local persisted Zustand/sync queue and accepted server protocol. Data: synthetic local cache/pending edits, slow/out-of-order polls, duplicate delivery, retained/expired cursor, offline restart. Debt: broad store rewrite excluded. Idempotency: repeated coherent responses converge; cursor advances only after successful ordered application.

- Given a coherent full/delta response containing deletions or authoritative absence, when the cache applies it, then all relevant slices and persisted state converge before cursor advancement; unrelated local drafts follow documented policy.
- Given a pending edit to a deleted entity, out-of-order response, failed application, or expired cursor, when synchronization proceeds, then no deleted row reappears, conflict/not-found is visible, and an explicit reset recovers a coherent state.
- **B-5-T1 [FEATURE; execution; M; confidence 2]** Implement ordered polling/cursor/snapshot-reset handling in the synchronization transport component; failing response-race/restart probes → transport → sync-client snapshot. Pull gate: B-4-T1/T2 accepted server contract and flag fallback. Unblocks: B-5-T2. Flag/rollback: reconciliation enablement only with compatible server; verified snapshot fallback.
- **B-5-T2 [FEATURE; execution; M; confidence 2]** Apply authoritative removals to the persisted store and resolve pending mutation conflicts without resurrection; failing cache/queue tests → reconciliation reducer/workflow → local-state evidence. Pull gate: B-5-T1 ordering contract and B-4-T1 collection/pending-write policy. Unblocks: B-6-T1. Flag/rollback: preserve coherent fallback; clear only reconstructable cache through documented reset, never silently discard user edits.

### B-6 — Demonstrate remediation across release boundaries (F1–F7)

As a Release Owner, I want independent evidence for permission, persistence, packaging, and client convergence so that a release claim matches supported runtime behavior. Value 3; priority H: acceptance gate, not a substitute for earlier fixes. Size M; confidence 2; **DoR HOLD until upstream contracts/implementations; external evidence may remain blocked**; INVEST I✓ N✓ V✓ E✓ S✓ T✓. Dependencies A-4 cloud fixture, SQL services, Linux CI, all implementation findings; availability recorded at gates. Data: isolated owner/manager/member sessions and disposable populated migration copies. Debt: no unverified finding marked resolved. Idempotency: verify stable operation/replay, write conflicts, repeated deletes, restart/reset; tests do not publish or deploy.

- Given packaged cloud sessions for a Member and an independent observer, when a check-in and deletion occur, then authoritative persisted facts survive reload/restart and the observer converges within the stated 10-second validation bound; denied changes remain denied.
- Given response loss, stale/legacy writers, migration/restore, and unavailable integration infrastructure, when evidence is assessed, then supported cases pass with artifacts and unavailable checks remain explicitly unpassed; no green baseline substitutes for closure.
- **B-6-T1 [PREPARATORY; independent review; M; confidence 2]** Write initially failing cloud multi-session scenario assertions, run accepted workflows with persisted-state checks, execute focused regression/full relevant suites and performance fixture, and inspect rollback/compatibility evidence; deliver evidence matrix and unresolved-gate list. Pull gate: A-1/A-2/A-3/A-4, B-1/B-2/B-3-T4/B-4-T3/T4/T5/B-5-T2 accepted interfaces and A-4-T2 test fixture. Unblocks: B-6-T2. Quality: `npm run lint`, `npm test`, `npm run test:bridge`, `npm run build`, relevant edition/protocol/agent checks; Windows 0600 issue visible and Linux check passes. Flag/rollback: exercise containment/fallback on disposable instances only.
- **B-6-T2 [PREPARATORY; independent review; S; confidence 3]** Reconcile F1–F7 evidence, runbook, ADRs, contract snapshots, debt decisions, and safe rollout/minimum-client instructions; start with acceptance checklist gaps, then publish the local release-readiness recommendation and epic phase summaries. Pull gate: B-6-T1 evidence including real drivers/clean images and all failure paths; missing runtime gate prevents release-ready status. Unblocks: END OF STORY SEQUENCE; later human-authorized release workflow. Rollback: explicitly record containment, retained schema, and operator restore data-loss window; no deployment/commit implied.

## Finding coverage and queue handoff

| Finding | Owning story | Closure evidence |
|---|---|---|
| F1 | A-1 | Real authenticated encoded-ID route plus unchanged denied-write state; B-6 review |
| F2 | A-2 | Positive/negative SQL corpus, both real driver enforcement/cleanup, all caller/bundle coverage |
| F3 | A-4 | Fresh client/cockpit image OIDC journeys and publication gates |
| F4 | B-3 | Atomic manual/live semantics, bounded retry identity, compatible client queue/response, B-6 observer journey |
| F5 | A-3 | Allowed full/restricted DTO controls and forbidden-field zero-write matrix |
| F6 | B-4/B-5 | All supported collection deletes, coherent ordering/reset/restart, pending-edit and two-session convergence |
| F7 | B-1/B-2 | Parent/children/history/version rollback on late failures and preserved conflict contracts |

First pull: **A-1-T1** after its baseline/contract check. Independent READY alternatives are A-2-T1, A-3-T1, A-4-T1, B-1-T1, and B-4-T1; choosing one does not clear any other gate. After Epic A, update phase summary/architecture memory and refine B using measured rework, cycle time, budget breaches, assumption failures, waiting, and handoff waste. Expand beyond these two epics only after that review.
